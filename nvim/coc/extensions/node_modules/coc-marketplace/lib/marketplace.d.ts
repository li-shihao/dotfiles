import { BasicList, ListContext, ListItem, Neovim } from 'coc.nvim';
interface IExtension {
    name: string;
    label: string;
    installed: boolean;
}
export default class Marketplace extends BasicList {
    readonly name = "marketplace";
    readonly description = "coc.nvim extensions marketplace";
    readonly detail = "display all coc.nvim extensions in list, with an install action";
    readonly defaultAction = "install";
    constructor(nvim: Neovim);
    loadItems(context: ListContext): Promise<ListItem[]>;
    fetchExtensions(): Promise<IExtension[]>;
    private format;
    doHighlight(): void;
}
export {};
