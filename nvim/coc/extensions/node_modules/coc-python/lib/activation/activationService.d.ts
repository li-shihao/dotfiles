import { Disposable } from 'vscode-languageserver-protocol';
import { Resource } from '../common/types';
import { IServiceContainer } from '../ioc/types';
import { IExtensionActivationService } from './types';
export declare class LanguageServerExtensionActivationService implements IExtensionActivationService, Disposable {
    private serviceContainer;
    private lsActivatedWorkspaces;
    private currentActivator?;
    private jediActivatedOnce;
    private readonly workspaceService;
    private readonly output;
    private readonly lsNotSupportedDiagnosticService;
    private resource;
    constructor(serviceContainer: IServiceContainer);
    activate(resource: Resource): Promise<void>;
    dispose(): void;
    protected onWorkspaceFoldersChanged(): void;
    private logStartup;
    private onDidChangeConfiguration;
    private useJedi;
    private getWorkspacePathKey;
}
