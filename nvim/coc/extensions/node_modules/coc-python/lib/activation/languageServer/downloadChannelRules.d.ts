import { IServiceContainer } from '../../ioc/types';
import { FolderVersionPair, IDownloadChannelRule } from '../types';
export declare class DownloadDailyChannelRule implements IDownloadChannelRule {
    shouldLookForNewLanguageServer(_currentFolder?: FolderVersionPair): Promise<boolean>;
}
export declare class DownloadStableChannelRule implements IDownloadChannelRule {
    shouldLookForNewLanguageServer(currentFolder?: FolderVersionPair): Promise<boolean>;
}
export declare class DownloadBetaChannelRule implements IDownloadChannelRule {
    private readonly serviceContainer;
    constructor(serviceContainer: IServiceContainer);
    shouldLookForNewLanguageServer(currentFolder?: FolderVersionPair): Promise<boolean>;
}
