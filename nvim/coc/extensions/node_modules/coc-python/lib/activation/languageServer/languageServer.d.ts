import { LanguageClientOptions } from 'coc.nvim';
import { IConfigurationService, Resource } from '../../common/types';
import { ILanguageClientFactory, ILanguageServer } from '../types';
export declare class LanguageServer implements ILanguageServer {
    private readonly factory;
    private readonly configurationService;
    private readonly startupCompleted;
    private readonly disposables;
    private languageClient?;
    private extensionLoadedArgs;
    constructor(factory: ILanguageClientFactory, configurationService: IConfigurationService);
    dispose(): void;
    start(resource: Resource, options: LanguageClientOptions): Promise<void>;
    loadExtension(args?: {}): void;
    protected serverReady(): Promise<void>;
}
