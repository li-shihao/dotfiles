import { Event, Emitter } from 'vscode-languageserver-protocol';
import { ICommandManager } from '../../common/application/types';
import { ILanguageServerExtension } from '../types';
export declare class LanguageServerExtension implements ILanguageServerExtension {
    private readonly commandManager;
    loadExtensionArgs?: {};
    protected readonly _invoked: Emitter<void>;
    private disposable?;
    constructor(commandManager: ICommandManager);
    dispose(): void;
    register(): Promise<void>;
    readonly invoked: Event<void>;
}
