import { Resource } from '../../common/types';
import { IServiceContainer } from '../../ioc/types';
import { ILanguageServerAnalysisOptions, ILanguageServerExtension, ILanguageServerManager } from '../types';
export declare class LanguageServerManager implements ILanguageServerManager {
    private readonly serviceContainer;
    private readonly analysisOptions;
    private readonly lsExtension;
    private languageServer?;
    private resource;
    private disposables;
    constructor(serviceContainer: IServiceContainer, analysisOptions: ILanguageServerAnalysisOptions, lsExtension: ILanguageServerExtension);
    dispose(): void;
    start(resource: Resource): Promise<void>;
    protected registerCommandHandler(): void;
    protected loadExtensionIfNecessary(): void;
    protected restartLanguageServerDebounced(): void;
    protected restartLanguageServer(): Promise<void>;
    protected startLanguageServer(): Promise<void>;
}
