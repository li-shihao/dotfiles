import { IOutputChannel, Resource } from '../../common/types';
import { IServiceContainer } from '../../ioc/types';
import { IApplicationDiagnostics } from '../types';
export declare class ApplicationDiagnostics implements IApplicationDiagnostics {
    private readonly serviceContainer;
    private readonly outputChannel;
    constructor(serviceContainer: IServiceContainer, outputChannel: IOutputChannel);
    register(): void;
    performPreStartupHealthCheck(resource: Resource): Promise<void>;
    private runDiagnostics;
    private log;
}
