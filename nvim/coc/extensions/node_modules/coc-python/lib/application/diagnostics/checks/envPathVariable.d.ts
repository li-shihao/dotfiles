import { Resource } from '../../../common/types';
import { IServiceContainer } from '../../../ioc/types';
import { BaseDiagnostic, BaseDiagnosticsService } from '../base';
import { MessageCommandPrompt } from '../promptHandler';
import { IDiagnostic, IDiagnosticHandlerService } from '../types';
export declare class InvalidEnvironmentPathVariableDiagnostic extends BaseDiagnostic {
    constructor(message: string, resource: Resource);
}
export declare const EnvironmentPathVariableDiagnosticsServiceId = "EnvironmentPathVariableDiagnosticsServiceId";
export declare class EnvironmentPathVariableDiagnosticsService extends BaseDiagnosticsService {
    protected readonly messageService: IDiagnosticHandlerService<MessageCommandPrompt>;
    private readonly platform;
    constructor(serviceContainer: IServiceContainer);
    diagnose(resource: Resource): Promise<IDiagnostic[]>;
    protected onHandle(diagnostics: IDiagnostic[]): Promise<void>;
    private doesPathVariableHaveInvalidEntries;
}
