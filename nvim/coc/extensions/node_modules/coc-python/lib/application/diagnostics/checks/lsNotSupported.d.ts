import { ILanguageServerCompatibilityService } from '../../../activation/types';
import { Resource } from '../../../common/types';
import { IServiceContainer } from '../../../ioc/types';
import { BaseDiagnostic, BaseDiagnosticsService } from '../base';
import { MessageCommandPrompt } from '../promptHandler';
import { IDiagnostic, IDiagnosticHandlerService } from '../types';
export declare class LSNotSupportedDiagnostic extends BaseDiagnostic {
    constructor(message: string, resource: Resource);
}
export declare const LSNotSupportedDiagnosticServiceId = "LSNotSupportedDiagnosticServiceId";
export declare class LSNotSupportedDiagnosticService extends BaseDiagnosticsService {
    private readonly lsCompatibility;
    protected readonly messageService: IDiagnosticHandlerService<MessageCommandPrompt>;
    constructor(serviceContainer: IServiceContainer, lsCompatibility: ILanguageServerCompatibilityService, messageService: IDiagnosticHandlerService<MessageCommandPrompt>);
    diagnose(resource: Resource): Promise<IDiagnostic[]>;
    protected onHandle(diagnostics: IDiagnostic[]): Promise<void>;
}
