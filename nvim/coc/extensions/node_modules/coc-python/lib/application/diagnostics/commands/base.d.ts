import { IDiagnostic, IDiagnosticCommand } from '../types';
export declare abstract class BaseDiagnosticCommand implements IDiagnosticCommand {
    readonly diagnostic: IDiagnostic;
    constructor(diagnostic: IDiagnostic);
    abstract invoke(): Promise<void>;
}
