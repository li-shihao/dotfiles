import { CommandsWithoutArgs } from '../../../common/application/commands';
import { IServiceContainer } from '../../../ioc/types';
import { IDiagnostic } from '../types';
import { BaseDiagnosticCommand } from './base';
export declare class ExecuteVSCCommand extends BaseDiagnosticCommand {
    private serviceContainer;
    private commandName;
    constructor(diagnostic: IDiagnostic, serviceContainer: IServiceContainer, commandName: CommandsWithoutArgs);
    invoke(): Promise<void>;
}
