import { IServiceContainer } from '../../../ioc/types';
import { DiagnosticScope, IDiagnostic } from '../types';
import { BaseDiagnosticCommand } from './base';
export declare class IgnoreDiagnosticCommand extends BaseDiagnosticCommand {
    private serviceContainer;
    private readonly scope;
    constructor(diagnostic: IDiagnostic, serviceContainer: IServiceContainer, scope: DiagnosticScope);
    invoke(): Promise<void>;
}
