import { IServiceContainer } from '../../../ioc/types';
import { IDiagnostic } from '../types';
import { BaseDiagnosticCommand } from './base';
export declare class LaunchBrowserCommand extends BaseDiagnosticCommand {
    private serviceContainer;
    private url;
    constructor(diagnostic: IDiagnostic, serviceContainer: IServiceContainer, url: string);
    invoke(): Promise<void>;
}
