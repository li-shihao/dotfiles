import { IServiceContainer } from '../../ioc/types';
import { DiagnosticScope, IDiagnosticFilterService } from './types';
export declare enum FilterKeys {
    GlobalDiagnosticFilter = "GLOBAL_DIAGNOSTICS_FILTER",
    WorkspaceDiagnosticFilter = "WORKSPACE_DIAGNOSTICS_FILTER"
}
export declare class DiagnosticFilterService implements IDiagnosticFilterService {
    private serviceContainer;
    constructor(serviceContainer: IServiceContainer);
    shouldIgnoreDiagnostic(code: string): Promise<boolean>;
    ignoreDiagnostic(code: string, scope: DiagnosticScope): Promise<void>;
}
