import { IServiceContainer } from '../../ioc/types';
import { IDiagnostic, IDiagnosticCommand, IDiagnosticHandlerService } from './types';
export interface MessageCommandPrompt {
    commandPrompts: {
        prompt: string;
        command?: IDiagnosticCommand;
    }[];
    message?: string;
}
export declare const DiagnosticCommandPromptHandlerServiceId = "DiagnosticCommandPromptHandlerServiceId";
export declare class DiagnosticCommandPromptHandlerService implements IDiagnosticHandlerService<MessageCommandPrompt> {
    private readonly appShell;
    constructor(serviceContainer: IServiceContainer);
    handle(diagnostic: IDiagnostic, options?: MessageCommandPrompt): Promise<void>;
    private showQuickPick;
    private displayMessage;
}
