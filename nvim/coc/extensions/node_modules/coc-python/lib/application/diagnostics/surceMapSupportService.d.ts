import { IApplicationShell, ICommandManager } from '../../common/application/types';
import { IConfigurationService, IDisposableRegistry } from '../../common/types';
import { ISourceMapSupportService } from './types';
export declare class SourceMapSupportService implements ISourceMapSupportService {
    private readonly commandManager;
    private readonly disposables;
    private readonly configurationService;
    private readonly shell;
    constructor(commandManager: ICommandManager, disposables: IDisposableRegistry, configurationService: IConfigurationService, shell: IApplicationShell);
    register(): void;
    enable(): Promise<void>;
    protected onEnable(): Promise<void>;
}
