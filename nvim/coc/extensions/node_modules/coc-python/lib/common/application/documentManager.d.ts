import { IDocumentManager } from './types';
import { TextDocument, Event, Position, WorkspaceEdit } from 'vscode-languageserver-protocol';
import Uri from 'vscode-uri';
export declare class DocumentManager implements IDocumentManager {
    readonly textDocuments: TextDocument[];
    readonly onDidOpenTextDocument: Event<TextDocument>;
    readonly onDidCloseTextDocument: Event<TextDocument>;
    readonly onDidSaveTextDocument: Event<TextDocument>;
    showTextDocument(document: TextDocument, position?: Position, preserveFocus?: boolean): Promise<number>;
    showTextDocument(document: TextDocument | Uri, opencmd?: string): Promise<number>;
    applyEdit(edit: WorkspaceEdit): Thenable<boolean>;
    openTextDocument(uri: Uri): Promise<TextDocument>;
}
