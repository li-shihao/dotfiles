import { Extension } from 'coc.nvim';
import { IExtensions } from '../types';
export declare class Extensions implements IExtensions {
    readonly all: Extension<any>[];
    getExtension(extensionId: any): Extension<any> | null;
}
