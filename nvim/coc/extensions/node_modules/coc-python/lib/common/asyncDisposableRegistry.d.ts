import { IAsyncDisposable, IAsyncDisposableRegistry, IDisposable } from './types';
export declare class AsyncDisposableRegistry implements IAsyncDisposableRegistry {
    private list;
    dispose(): Promise<void>;
    push(disposable?: IDisposable | IAsyncDisposable): void;
}
