import { ICommandManager } from './application/types';
export declare class ContextKey {
    private name;
    private commandManager;
    private lastValue?;
    constructor(name: string, commandManager: ICommandManager);
    set(value: boolean): Promise<void>;
}
