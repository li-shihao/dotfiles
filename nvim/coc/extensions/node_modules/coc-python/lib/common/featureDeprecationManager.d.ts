import { WorkspaceConfiguration } from 'coc.nvim';
import { IApplicationShell, ICommandManager, IWorkspaceService } from './application/types';
import { DeprecatedFeatureInfo, DeprecatedSettingAndValue, IFeatureDeprecationManager, IPersistentStateFactory } from './types';
export declare class FeatureDeprecationManager implements IFeatureDeprecationManager {
    private persistentStateFactory;
    private cmdMgr;
    private workspace;
    private appShell;
    private disposables;
    constructor(persistentStateFactory: IPersistentStateFactory, cmdMgr: ICommandManager, workspace: IWorkspaceService, appShell: IApplicationShell);
    dispose(): void;
    initialize(): void;
    registerDeprecation(deprecatedInfo: DeprecatedFeatureInfo): void;
    notifyDeprecation(deprecatedInfo: DeprecatedFeatureInfo): Promise<void>;
    checkAndNotifyDeprecatedSetting(deprecatedInfo: DeprecatedFeatureInfo): void;
    isDeprecatedSettingAndValueUsed(pythonConfig: WorkspaceConfiguration, deprecatedSetting: DeprecatedSettingAndValue): boolean;
}
