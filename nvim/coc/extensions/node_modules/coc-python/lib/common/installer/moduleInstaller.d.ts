import Uri from 'vscode-uri';
import { IServiceContainer } from '../../ioc/types';
import { ExecutionInfo } from '../types';
export declare abstract class ModuleInstaller {
    protected serviceContainer: IServiceContainer;
    constructor(serviceContainer: IServiceContainer);
    installModule(name: string, resource?: Uri): Promise<void>;
    abstract isSupported(resource?: Uri): Promise<boolean>;
    protected abstract getExecutionInfo(moduleName: string, resource?: Uri): Promise<ExecutionInfo>;
    private processInstallArgs;
    private isPathWritableAsync;
    private elevatedInstall;
}
