import Uri from 'vscode-uri';
import { IServiceContainer } from '../../ioc/types';
import { ExecutionInfo } from '../types';
import { ModuleInstaller } from './moduleInstaller';
import { IModuleInstaller } from './types';
export declare class PipInstaller extends ModuleInstaller implements IModuleInstaller {
    readonly displayName: string;
    readonly priority: number;
    constructor(serviceContainer: IServiceContainer);
    isSupported(resource?: Uri): Promise<boolean>;
    protected getExecutionInfo(moduleName: string, _resource?: Uri): Promise<ExecutionInfo>;
    private isPipAvailable;
}
