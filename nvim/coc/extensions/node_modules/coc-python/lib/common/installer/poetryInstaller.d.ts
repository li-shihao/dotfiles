import Uri from 'vscode-uri';
import { IServiceContainer } from '../../ioc/types';
import { IWorkspaceService } from '../application/types';
import { IFileSystem } from '../platform/types';
import { IProcessServiceFactory } from '../process/types';
import { ExecutionInfo, IConfigurationService } from '../types';
import { ModuleInstaller } from './moduleInstaller';
import { IModuleInstaller } from './types';
export declare const poetryName = "poetry";
export declare class PoetryInstaller extends ModuleInstaller implements IModuleInstaller {
    private readonly workspaceService;
    private readonly configurationService;
    private readonly fs;
    private readonly processFactory;
    readonly displayName: string;
    readonly priority: number;
    constructor(serviceContainer: IServiceContainer, workspaceService: IWorkspaceService, configurationService: IConfigurationService, fs: IFileSystem, processFactory: IProcessServiceFactory);
    isSupported(resource?: Uri): Promise<boolean>;
    protected isPoetryAvailable(workfolder: Uri): Promise<boolean>;
    protected getExecutionInfo(moduleName: string, resource?: Uri): Promise<ExecutionInfo>;
}
