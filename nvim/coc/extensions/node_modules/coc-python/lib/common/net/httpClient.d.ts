import requestTypes from 'request';
import { IHttpClient } from '../../activation/types';
export declare class HttpClient implements IHttpClient {
    readonly requestOptions: requestTypes.CoreOptions;
    constructor();
    downloadFile(uri: string): Promise<requestTypes.Request>;
    getJSON<T>(uri: string): Promise<T>;
}
