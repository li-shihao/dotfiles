/// <reference types="node" />
import { EventEmitter } from "events";
import { SocketStream } from "./SocketStream";
import { SocketServer } from './socketServer';
export declare abstract class SocketCallbackHandler extends EventEmitter {
    private _stream;
    private commandHandlers;
    private handeshakeDone;
    constructor(socketServer: SocketServer);
    private disposed;
    dispose(): void;
    private onData;
    protected readonly stream: SocketStream;
    protected SendRawCommand(commandId: Buffer): void;
    protected registerCommandHandler(commandId: string, handler: Function): void;
    protected abstract handleHandshake(): boolean;
    private HandleIncomingData;
    private HandleIncomingDataFromStream;
}
