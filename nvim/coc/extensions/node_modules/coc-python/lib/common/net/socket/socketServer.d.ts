/// <reference types="node" />
import { EventEmitter } from 'events';
import * as net from 'net';
import { ISocketServer } from '../../types';
export declare class SocketServer extends EventEmitter implements ISocketServer {
    private socketServer;
    private clientSocket;
    readonly client: Promise<net.Socket>;
    constructor();
    dispose(): void;
    Stop(): void;
    Start(options?: {
        port?: number;
        host?: string;
    }): Promise<number>;
    private connectionListener;
}
