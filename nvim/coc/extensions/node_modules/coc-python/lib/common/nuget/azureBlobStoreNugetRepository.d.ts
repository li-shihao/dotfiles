import { IServiceContainer } from '../../ioc/types';
import { INugetRepository, NugetPackage } from './types';
export declare class AzureBlobStoreNugetRepository implements INugetRepository {
    private readonly serviceContainer;
    protected readonly azureBlobStorageAccount: string;
    protected readonly azureBlobStorageContainer: string;
    protected readonly azureCDNBlobStorageAccount: string;
    constructor(serviceContainer: IServiceContainer, azureBlobStorageAccount: string, azureBlobStorageContainer: string, azureCDNBlobStorageAccount: string);
    getPackages(packageName: string): Promise<NugetPackage[]>;
    protected listPackages(azureBlobStorageAccount: string, azureBlobStorageContainer: string, packageName: string, azureCDNBlobStorageAccount: string): Promise<NugetPackage[]>;
}
