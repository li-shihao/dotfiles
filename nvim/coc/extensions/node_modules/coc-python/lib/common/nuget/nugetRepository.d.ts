import { SemVer } from 'semver';
import { IServiceContainer } from '../../ioc/types';
import { INugetRepository, NugetPackage } from './types';
export declare class NugetRepository implements INugetRepository {
    private readonly serviceContainer;
    constructor(serviceContainer: IServiceContainer);
    getPackages(packageName: string): Promise<NugetPackage[]>;
    getVersions(packageBaseAddress: string, packageName: string): Promise<SemVer[]>;
    getNugetPackageUri(packageBaseAddress: string, packageName: string, version: SemVer): string;
}
