import { IPathUtils } from '../types';
export declare class PathUtils implements IPathUtils {
    private isWindows;
    readonly home = "";
    constructor(isWindows: boolean);
    readonly delimiter: string;
    readonly separator: string;
    getPathVariableName(): "Path" | "PATH";
    basename(pathValue: string, ext?: string): string;
    getDisplayName(pathValue: string, cwd?: string): string;
}
