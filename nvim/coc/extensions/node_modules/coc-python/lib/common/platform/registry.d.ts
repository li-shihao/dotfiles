import { Architecture } from '../utils/platform';
import { IRegistry, RegistryHive } from './types';
export declare class RegistryImplementation implements IRegistry {
    getKeys(key: string, hive: RegistryHive, arch?: Architecture): Promise<string[]>;
    getValue(key: string, hive: RegistryHive, arch?: Architecture, name?: string): Promise<string>;
}
export declare function getArchitectureDisplayName(arch?: Architecture): "" | "64-bit" | "32-bit";
