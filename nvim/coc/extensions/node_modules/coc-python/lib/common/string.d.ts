/**
 * Split a string using the cr and lf characters and return them as an array.
 * By default lines are trimmed and empty lines are removed.
 * @param {SplitLinesOptions=} splitOptions - Options used for splitting the string.
 */
export declare function splitLines(str: string, splitOptions?: {
    trim: boolean;
    removeEmptyEntries: boolean;
}): string[];
/**
 * Appropriately formats a string so it can be used as an argument for a command in a shell.
 * E.g. if an argument contains a space, then it will be enclosed within double quotes.
 * @param {String} value.
 */
export declare function toCommandArgument(str: string): string;
/**
 * Appropriately formats a a file path so it can be used as an argument for a command in a shell.
 * E.g. if an argument contains a space, then it will be enclosed within double quotes.
 */
export declare function fileToCommandArgument(str: string): string;
/**
 * String.trimQuotes implementation
 * Removes leading and trailing quotes from a string
 */
export declare function trimQuotes(str: string): string;
export declare function format(str: string, ...args: any[]): string;
