import { Terminal, Uri } from 'coc.nvim';
import { ITerminalActivator, ITerminalHelper, TerminalShellType } from '../types';
export declare class BaseTerminalActivator implements ITerminalActivator {
    private readonly helper;
    private readonly activatedTerminals;
    constructor(helper: ITerminalHelper);
    activateEnvironmentInTerminal(terminal: Terminal, resource: Uri | undefined, preserveFocus?: boolean): Promise<boolean>;
    protected waitForCommandToProcess(_shell: TerminalShellType): Promise<void>;
}
