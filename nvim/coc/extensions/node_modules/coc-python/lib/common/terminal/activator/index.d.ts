import { Terminal, Uri } from 'coc.nvim';
import { ITerminalActivationHandler, ITerminalActivator, ITerminalHelper } from '../types';
export declare class TerminalActivator implements ITerminalActivator {
    readonly helper: ITerminalHelper;
    private readonly handlers;
    protected baseActivator: ITerminalActivator;
    constructor(helper: ITerminalHelper, handlers: ITerminalActivationHandler[]);
    activateEnvironmentInTerminal(terminal: Terminal, resource: Uri | undefined, preserveFocus?: boolean): Promise<boolean>;
    protected initialize(): void;
}
