import { Terminal } from 'coc.nvim';
import { IDiagnosticsService } from '../../../application/diagnostics/types';
import { IPlatformService } from '../../platform/types';
import { Resource } from '../../types';
import { ITerminalActivationHandler, ITerminalHelper } from '../types';
export declare class PowershellTerminalActivationFailedHandler implements ITerminalActivationHandler {
    private readonly helper;
    private readonly platformService;
    private readonly diagnosticService;
    constructor(helper: ITerminalHelper, platformService: IPlatformService, diagnosticService: IDiagnosticsService);
    handleActivation(_terminal: Terminal, resource: Resource, _preserveFocus: boolean, activated: boolean): Promise<void>;
}
