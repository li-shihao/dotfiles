import Uri from 'vscode-uri';
import { IServiceContainer } from '../../../ioc/types';
import { ITerminalActivationCommandProvider, TerminalShellType } from '../types';
export declare abstract class BaseActivationCommandProvider implements ITerminalActivationCommandProvider {
    protected readonly serviceContainer: IServiceContainer;
    constructor(serviceContainer: IServiceContainer);
    abstract isShellSupported(targetShell: TerminalShellType): boolean;
    getActivationCommands(resource: Uri | undefined, targetShell: TerminalShellType): Promise<string[] | undefined>;
    abstract getActivationCommandsForInterpreter(pythonPath: string, targetShell: TerminalShellType): Promise<string[] | undefined>;
    protected findScriptFile(pythonPath: string, scriptFileNames: string[]): Promise<string | undefined>;
}
