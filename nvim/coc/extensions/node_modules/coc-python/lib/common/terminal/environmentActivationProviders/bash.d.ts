import { IServiceContainer } from '../../../ioc/types';
import { TerminalShellType } from '../types';
import { BaseActivationCommandProvider } from './baseActivationProvider';
export declare class Bash extends BaseActivationCommandProvider {
    constructor(serviceContainer: IServiceContainer);
    isShellSupported(targetShell: TerminalShellType): boolean;
    getActivationCommandsForInterpreter(pythonPath: string, targetShell: TerminalShellType): Promise<string[] | undefined>;
    private getScriptsInOrderOfPreference;
}
