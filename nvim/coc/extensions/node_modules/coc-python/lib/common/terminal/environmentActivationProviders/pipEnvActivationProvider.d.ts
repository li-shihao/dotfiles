import Uri from 'vscode-uri';
import { IInterpreterService, IPipEnvService } from '../../../interpreter/contracts';
import { IWorkspaceService } from '../../application/types';
import { IFileSystem } from '../../platform/types';
import { ITerminalActivationCommandProvider, TerminalShellType } from '../types';
export declare class PipEnvActivationCommandProvider implements ITerminalActivationCommandProvider {
    private readonly interpreterService;
    private readonly pipenvService;
    private readonly workspaceService;
    private readonly fs;
    constructor(interpreterService: IInterpreterService, pipenvService: IPipEnvService, workspaceService: IWorkspaceService, fs: IFileSystem);
    isShellSupported(_targetShell: TerminalShellType): boolean;
    getActivationCommands(resource: Uri | undefined, _: TerminalShellType): Promise<string[] | undefined>;
    getActivationCommandsForInterpreter(pythonPath: string, _targetShell: TerminalShellType): Promise<string[] | undefined>;
}
