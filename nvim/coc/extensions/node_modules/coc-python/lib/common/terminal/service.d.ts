import { Disposable, Event } from 'vscode-languageserver-protocol';
import Uri from 'vscode-uri';
import { IServiceContainer } from '../../ioc/types';
import { ITerminalService } from './types';
export declare class TerminalService implements ITerminalService, Disposable {
    private serviceContainer;
    private resource?;
    private title;
    private terminal?;
    private terminalShellType;
    private terminalClosed;
    private terminalManager;
    private terminalHelper;
    private terminalActivator;
    readonly onDidCloseTerminal: Event<void>;
    constructor(serviceContainer: IServiceContainer, resource?: Uri, title?: string);
    dispose(): void;
    sendCommand(command: string, args: string[]): Promise<void>;
    sendText(text: string): Promise<void>;
    show(preserveFocus?: boolean): Promise<void>;
    private ensureTerminal;
    private terminalCloseHandler;
}
