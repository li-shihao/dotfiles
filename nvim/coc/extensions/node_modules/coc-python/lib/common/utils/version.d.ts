import * as semver from 'semver';
import { Version } from '../types';
export declare function parseVersion(raw: string): semver.SemVer;
export declare function parsePythonVersion(version: string): Version | undefined;
