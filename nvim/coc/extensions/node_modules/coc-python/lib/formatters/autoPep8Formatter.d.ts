import { IServiceContainer } from '../ioc/types';
import { BaseFormatter } from './baseFormatter';
import { TextDocument, TextEdit, FormattingOptions, CancellationToken, Range } from 'vscode-languageserver-protocol';
export declare class AutoPep8Formatter extends BaseFormatter {
    constructor(serviceContainer: IServiceContainer);
    formatDocument(document: TextDocument, options: FormattingOptions, token: CancellationToken, range?: Range): Thenable<TextEdit[]>;
}
