import { IServiceContainer } from '../ioc/types';
import { BaseFormatter } from './baseFormatter';
import { TextDocument, FormattingOptions, Range, TextEdit } from 'vscode-languageserver-types';
import { CancellationToken } from 'vscode-jsonrpc';
export declare class DummyFormatter extends BaseFormatter {
    constructor(serviceContainer: IServiceContainer);
    formatDocument(_document: TextDocument, _options: FormattingOptions, _token: CancellationToken, _range?: Range): Thenable<TextEdit[]>;
}
