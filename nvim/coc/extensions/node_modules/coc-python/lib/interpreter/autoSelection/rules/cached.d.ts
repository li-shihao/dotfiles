import { IFileSystem } from '../../../common/platform/types';
import { IPersistentStateFactory, Resource } from '../../../common/types';
import { IInterpreterHelper } from '../../contracts';
import { IInterpreterAutoSelectionRule, IInterpreterAutoSelectionService } from '../types';
import { BaseRuleService, NextAction } from './baseRule';
export declare class CachedInterpretersAutoSelectionRule extends BaseRuleService {
    private readonly helper;
    protected readonly rules: IInterpreterAutoSelectionRule[];
    constructor(fs: IFileSystem, helper: IInterpreterHelper, stateFactory: IPersistentStateFactory, systemInterpreter: IInterpreterAutoSelectionRule, winRegInterpreter: IInterpreterAutoSelectionRule);
    protected onAutoSelectInterpreter(resource: Resource, manager?: IInterpreterAutoSelectionService): Promise<NextAction>;
}
