import { IWorkspaceService } from '../../../common/application/types';
import { IFileSystem } from '../../../common/platform/types';
import { IPersistentStateFactory, Resource } from '../../../common/types';
import { IInterpreterAutoSelectionService } from '../types';
import { BaseRuleService, NextAction } from './baseRule';
export declare class SettingsInterpretersAutoSelectionRule extends BaseRuleService {
    private readonly workspaceService;
    constructor(fs: IFileSystem, stateFactory: IPersistentStateFactory, workspaceService: IWorkspaceService);
    protected onAutoSelectInterpreter(_resource: Resource, _manager?: IInterpreterAutoSelectionService): Promise<NextAction>;
}
