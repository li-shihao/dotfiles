import { IInterpreterHelper, PythonInterpreter } from '../contracts';
import { IInterpreterComparer } from './types';
export declare class InterpreterComparer implements IInterpreterComparer {
    private readonly interpreterHelper;
    constructor(interpreterHelper: IInterpreterHelper);
    compare(a: PythonInterpreter, b: PythonInterpreter): number;
    private getSortName;
}
