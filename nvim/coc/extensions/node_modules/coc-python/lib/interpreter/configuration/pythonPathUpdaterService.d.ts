import { ConfigurationTarget, Uri } from 'coc.nvim';
import { IServiceContainer } from '../../ioc/types';
import { IPythonPathUpdaterServiceManager } from './types';
export declare class PythonPathUpdaterService implements IPythonPathUpdaterServiceManager {
    private readonly pythonPathSettingsUpdaterFactory;
    constructor(serviceContainer: IServiceContainer);
    updatePythonPath(pythonPath: string, configTarget: ConfigurationTarget, trigger: 'ui' | 'shebang' | 'load', wkspace?: Uri): Promise<void>;
    private getPythonUpdaterService;
}
