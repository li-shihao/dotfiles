import { IPythonPathUpdaterService } from '../types';
import { IPersistentState } from '../../../common/types';
export declare class GlobalPythonPathUpdaterService implements IPythonPathUpdaterService {
    private readonly store;
    constructor(store: IPersistentState<string | undefined>);
    updatePythonPath(pythonPath: string, trigger: 'ui' | 'shebang' | 'load'): Promise<void>;
}
