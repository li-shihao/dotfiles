import { Uri } from 'coc.nvim';
import { IPersistentState } from '../../../common/types';
import { IPythonPathUpdaterService } from '../types';
export declare class WorkspaceFolderPythonPathUpdaterService implements IPythonPathUpdaterService {
    private workspaceFolder;
    private readonly workspaceStore;
    constructor(workspaceFolder: Uri, workspaceStore: IPersistentState<string | undefined>);
    updatePythonPath(pythonPath: string, trigger: 'ui' | 'shebang' | 'load'): Promise<void>;
}
