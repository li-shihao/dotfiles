import { Uri } from 'coc.nvim';
import { IServiceContainer } from '../../ioc/types';
import { IInterpreterDisplay } from '../contracts';
export declare class InterpreterDisplay implements IInterpreterDisplay {
    private readonly statusBar;
    private readonly helper;
    private readonly workspaceService;
    private readonly interpreterService;
    private currentlySelectedInterpreterPath?;
    private currentlySelectedWorkspaceFolder;
    private readonly autoSelection;
    constructor(serviceContainer: IServiceContainer);
    refresh(resource?: Uri): Promise<void>;
    private onDidChangeInterpreterInformation;
    private updateDisplay;
}
