import { Disposable } from 'coc.nvim';
import { IApplicationShell } from '../../common/application/types';
import { IInterpreterLocatorProgressService, InterpreterLocatorProgressHandler } from '../contracts';
export declare class InterpreterLocatorProgressStatubarHandler implements InterpreterLocatorProgressHandler {
    private readonly shell;
    private readonly progressService;
    private readonly disposables;
    private item;
    constructor(shell: IApplicationShell, progressService: IInterpreterLocatorProgressService, disposables: Disposable[]);
    register(): void;
    private showProgress;
    private hideProgress;
}
