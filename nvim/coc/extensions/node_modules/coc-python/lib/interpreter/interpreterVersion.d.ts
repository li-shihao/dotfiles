import { IProcessServiceFactory } from '../common/process/types';
import { IInterpreterVersionService } from './contracts';
export declare const PIP_VERSION_REGEX = "\\d+\\.\\d+(\\.\\d+)?";
export declare class InterpreterVersionService implements IInterpreterVersionService {
    private readonly processServiceFactory;
    constructor(processServiceFactory: IProcessServiceFactory);
    getVersion(pythonPath: string, defaultValue: string): Promise<string>;
    getPipVersion(pythonPath: string): Promise<string>;
}
