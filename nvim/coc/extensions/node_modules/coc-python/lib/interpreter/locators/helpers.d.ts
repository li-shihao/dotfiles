import { IFileSystem } from '../../common/platform/types';
import { IInterpreterLocatorHelper, PythonInterpreter } from '../contracts';
import { IPipEnvServiceHelper } from './types';
export declare function lookForInterpretersInDirectory(pathToCheck: string): Promise<string[]>;
export declare class InterpreterLocatorHelper implements IInterpreterLocatorHelper {
    private readonly fs;
    private readonly pipEnvServiceHelper;
    constructor(fs: IFileSystem, pipEnvServiceHelper: IPipEnvServiceHelper);
    mergeInterpreters(interpreters: PythonInterpreter[]): Promise<PythonInterpreter[]>;
}
