import { Disposable, Event } from 'coc.nvim';
import { IServiceContainer } from '../../ioc/types';
import { IInterpreterLocatorProgressService } from '../contracts';
export declare class InterpreterLocatorProgressService implements IInterpreterLocatorProgressService {
    private readonly disposables;
    private deferreds;
    private readonly refreshing;
    private readonly refreshed;
    private readonly locators;
    constructor(serviceContainer: IServiceContainer, disposables: Disposable[]);
    readonly onRefreshing: Event<void>;
    readonly onRefreshed: Event<void>;
    register(): void;
    private handleProgress;
    private notifyCompleted;
    private notifyRefreshing;
    private checkProgress;
    private areAllItemsComplete;
}
