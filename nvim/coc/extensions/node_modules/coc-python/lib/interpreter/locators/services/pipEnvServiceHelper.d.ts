import { Uri } from 'coc.nvim';
import { IFileSystem } from '../../../common/platform/types';
import { IPersistentStateFactory } from '../../../common/types';
import { IPipEnvServiceHelper } from '../types';
export declare class PipEnvServiceHelper implements IPipEnvServiceHelper {
    private readonly statefactory;
    private readonly fs;
    private initialized;
    private readonly state;
    constructor(statefactory: IPersistentStateFactory, fs: IFileSystem);
    getPipEnvInfo(pythonPath: string): Promise<{
        workspaceFolder: Uri;
        envName: string;
    } | undefined>;
    trackWorkspaceFolder(pythonPath: string, workspaceFolder: Uri): Promise<void>;
    protected initializeStateStore(): Promise<void>;
}
