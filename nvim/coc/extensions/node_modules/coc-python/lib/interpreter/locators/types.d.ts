import { Uri } from 'coc.nvim';
export declare const IPythonInPathCommandProvider: unique symbol;
export interface IPythonInPathCommandProvider {
    getCommands(): {
        command: string;
        args?: string[];
    }[];
}
export declare const IPipEnvServiceHelper: unique symbol;
export interface IPipEnvServiceHelper {
    getPipEnvInfo(pythonPath: string): Promise<{
        workspaceFolder: Uri;
        envName: string;
    } | undefined>;
    trackWorkspaceFolder(pythonPath: string, workspaceFolder: Uri): Promise<void>;
}
