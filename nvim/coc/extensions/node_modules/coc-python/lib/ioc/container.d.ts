import { Container, interfaces } from 'inversify';
import { Abstract, IServiceContainer, Newable } from './types';
export declare class ServiceContainer implements IServiceContainer {
    private container;
    constructor(container: Container);
    get<T>(serviceIdentifier: interfaces.ServiceIdentifier<T>, name?: string | number | symbol): T;
    getAll<T>(serviceIdentifier: string | symbol | Newable<T> | Abstract<T>, name?: string | number | symbol | undefined): T[];
}
