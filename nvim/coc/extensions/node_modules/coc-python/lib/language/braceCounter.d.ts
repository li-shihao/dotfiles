import { IToken, TokenType } from './types';
export declare class BraceCounter {
    private readonly bracePairs;
    private braceStacks;
    readonly count: number;
    isOpened(type: TokenType): boolean;
    countBrace(brace: IToken): boolean;
}
