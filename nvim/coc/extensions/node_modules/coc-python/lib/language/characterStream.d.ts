import { ICharacterStream, ITextIterator } from './types';
export declare class CharacterStream implements ICharacterStream {
    private text;
    private _position;
    private _currentChar;
    private _isEndOfStream;
    constructor(text: string | ITextIterator);
    getText(): string;
    position: number;
    readonly currentChar: number;
    readonly nextChar: number;
    readonly prevChar: number;
    isEndOfStream(): boolean;
    lookAhead(offset: number): number;
    advance(offset: number): void;
    moveNext(): boolean;
    isAtWhiteSpace(): boolean;
    isAtLineBreak(): boolean;
    skipLineBreak(): void;
    skipWhitespace(): void;
    skipToEol(): void;
    skipToWhitespace(): void;
    isAtString(): boolean;
    charCodeAt(index: number): number;
    readonly length: number;
    private checkBounds;
}
