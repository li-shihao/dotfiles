import { ITextRange, ITextRangeCollection } from './types';
export declare class IterableTextRange<T extends ITextRange> implements Iterable<T> {
    private textRangeCollection;
    constructor(textRangeCollection: ITextRangeCollection<T>);
    [Symbol.iterator](): Iterator<T>;
}
