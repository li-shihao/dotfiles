import { ITextRange, ITextRangeCollection } from './types';
export declare class TextRangeCollection<T extends ITextRange> implements ITextRangeCollection<T> {
    private items;
    constructor(items: T[]);
    readonly start: number;
    readonly end: number;
    readonly length: number;
    readonly count: number;
    contains(position: number): boolean;
    getItemAt(index: number): T;
    getItemAtPosition(position: number): number;
    getItemContaining(position: number): number;
}
