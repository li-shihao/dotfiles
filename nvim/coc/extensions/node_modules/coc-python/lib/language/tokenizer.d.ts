import { ITextRangeCollection, IToken, ITokenizer, TokenizerMode } from './types';
export declare class Tokenizer implements ITokenizer {
    private cs;
    private tokens;
    private mode;
    tokenize(text: string): ITextRangeCollection<IToken>;
    tokenize(text: string, start: number, length: number, mode: TokenizerMode): ITextRangeCollection<IToken>;
    private AddNextToken;
    private handleCharacter;
    private tryIdentifier;
    private isPossibleNumber;
    private tryNumber;
    private tryOperator;
    private handleUnknown;
    private handleComment;
    private getStringPrefixLength;
    private getQuoteType;
    private handleString;
    private skipToSingleEndQuote;
    private skipToTripleEndQuote;
    private skipFloatingPointCandidate;
    private skipFractionalNumber;
    private skipDecimalNumber;
}
