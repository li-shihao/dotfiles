import { Disposable, Uri } from 'coc.nvim';
import { IServiceContainer } from '../ioc/types';
import { ICommandResult, JediProxyHandler } from '../providers/jediProxy';
export declare class JediFactory implements Disposable {
    private extensionRootPath;
    private serviceContainer;
    private disposables;
    private jediProxyHandlers;
    constructor(extensionRootPath: string, serviceContainer: IServiceContainer);
    dispose(): void;
    getJediProxyHandler<T extends ICommandResult>(resource?: Uri): JediProxyHandler<T>;
}
