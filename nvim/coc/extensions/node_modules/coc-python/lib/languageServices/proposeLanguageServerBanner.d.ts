import { IApplicationShell } from '../common/application/types';
import { IConfigurationService, IPersistentStateFactory, IPythonExtensionBanner } from '../common/types';
export declare enum ProposeLSStateKeys {
    ShowBanner = "ProposeLSBanner"
}
export declare class ProposeLanguageServerBanner implements IPythonExtensionBanner {
    private appShell;
    private persistentState;
    private configuration;
    private initialized?;
    private disabledInCurrentSession;
    private sampleSizePerHundred;
    private bannerMessage;
    private bannerLabels;
    constructor(appShell: IApplicationShell, persistentState: IPersistentStateFactory, configuration: IConfigurationService, sampleSizePerOneHundredUsers?: number);
    initialize(): void;
    readonly enabled: boolean;
    showBanner(): Promise<void>;
    shouldShowBanner(): Promise<boolean>;
    disable(): Promise<void>;
    enableNewLanguageServer(): Promise<void>;
}
