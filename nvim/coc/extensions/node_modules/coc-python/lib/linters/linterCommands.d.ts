import { DiagnosticCollection } from 'coc.nvim';
import { IDisposable } from '../common/types';
import { IServiceContainer } from '../ioc/types';
export declare class LinterCommands implements IDisposable {
    private serviceContainer;
    private disposables;
    private linterManager;
    constructor(serviceContainer: IServiceContainer);
    dispose(): void;
    setLinterAsync(): Promise<void>;
    enableLintingAsync(): Promise<void>;
    runLinting(): Promise<DiagnosticCollection>;
    private readonly settingsUri;
}
