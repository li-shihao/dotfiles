import { DiagnosticCollection, Extension } from 'coc.nvim';
import { TextDocument } from 'vscode-languageserver-protocol';
import { IServiceContainer } from '../ioc/types';
import { ILintingEngine } from './types';
export declare class LintingEngine implements ILintingEngine {
    private serviceContainer;
    private documentHasJupyterCodeCells;
    private documents;
    private configurationService;
    private linterManager;
    private diagnosticCollection;
    private pendingLintings;
    private outputChannel;
    private fileSystem;
    constructor(serviceContainer: IServiceContainer);
    readonly diagnostics: DiagnosticCollection;
    clearDiagnostics(document: TextDocument): void;
    lintOpenPythonFiles(): Promise<DiagnosticCollection>;
    lintDocument(document: TextDocument): Promise<void>;
    linkJupyterExtension(jupyter: Extension<any> | undefined): Promise<void>;
    private isDocumentOpen;
    private createDiagnostics;
    private shouldLintDocument;
}
