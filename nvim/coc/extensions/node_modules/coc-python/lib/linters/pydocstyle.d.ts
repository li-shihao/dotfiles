import { CancellationToken, TextDocument } from 'vscode-languageserver-protocol';
import { OutputChannel } from 'coc.nvim';
import { IServiceContainer } from '../ioc/types';
import { BaseLinter } from './baseLinter';
import { ILintMessage } from './types';
export declare class PyDocStyle extends BaseLinter {
    constructor(outputChannel: OutputChannel, serviceContainer: IServiceContainer);
    protected runLinter(document: TextDocument, cancellation: CancellationToken): Promise<ILintMessage[]>;
    protected parseMessages(output: string, document: TextDocument, _token: CancellationToken, _regEx: string): Promise<ILintMessage[]>;
}
