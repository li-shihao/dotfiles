import { IServiceContainer } from '../ioc/types';
import { JediFactory } from '../languageServices/jediProxyFactory';
import { CompletionItemProvider } from 'coc.nvim';
import { TextDocument, Position, CancellationToken, CompletionItem } from 'vscode-languageserver-protocol';
export declare class PythonCompletionItemProvider implements CompletionItemProvider {
    private completionSource;
    private configService;
    constructor(jediFactory: JediFactory, serviceContainer: IServiceContainer);
    provideCompletionItems(document: TextDocument, position: Position, token: CancellationToken): Promise<CompletionItem[]>;
    resolveCompletionItem(item: CompletionItem, token: CancellationToken): Promise<CompletionItem>;
}
