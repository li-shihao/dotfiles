import { IServiceContainer } from '../ioc/types';
import { JediFactory } from '../languageServices/jediProxyFactory';
import { IItemInfoSource, LanguageItemInfo } from './itemInfoSource';
import { TextDocument, Position, CancellationToken, CompletionItem } from 'vscode-languageserver-protocol';
export declare class CompletionSource {
    private serviceContainer;
    private itemInfoSource;
    private jediFactory;
    constructor(jediFactory: JediFactory, serviceContainer: IServiceContainer, itemInfoSource: IItemInfoSource);
    getVsCodeCompletionItems(document: TextDocument, position: Position, token: CancellationToken): Promise<CompletionItem[]>;
    getDocumentation(completionItem: CompletionItem, token: CancellationToken): Promise<LanguageItemInfo[] | undefined>;
    private getCompletionResult;
    private toVsCodeCompletions;
    private toVsCodeCompletion;
}
