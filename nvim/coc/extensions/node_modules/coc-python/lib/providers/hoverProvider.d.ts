import { JediFactory } from '../languageServices/jediProxyFactory';
import { HoverProvider } from 'coc.nvim';
import { TextDocument, Position, CancellationToken, Hover } from 'vscode-languageserver-protocol';
export declare class PythonHoverProvider implements HoverProvider {
    private itemInfoSource;
    constructor(jediFactory: JediFactory);
    provideHover(document: TextDocument, position: Position, token: CancellationToken): Promise<Hover | undefined>;
}
