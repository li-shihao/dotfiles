import { Uri } from 'coc.nvim';
import { CancellationToken, WorkspaceEdit } from 'vscode-languageserver-protocol';
import { IServiceContainer } from '../ioc/types';
import { ISortImportsEditingProvider } from './types';
export declare class SortImportsEditingProvider implements ISortImportsEditingProvider {
    private serviceContainer;
    private readonly processServiceFactory;
    private readonly pythonExecutionFactory;
    private readonly documentManager;
    private readonly configurationService;
    private readonly editorUtils;
    constructor(serviceContainer: IServiceContainer);
    provideDocumentSortImportsEdits(uri: Uri, token?: CancellationToken): Promise<WorkspaceEdit | undefined>;
    registerCommands(): void;
    sortImports(uri?: Uri): Promise<void>;
}
