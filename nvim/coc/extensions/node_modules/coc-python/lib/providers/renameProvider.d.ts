import { ProviderResult, RenameProvider } from 'coc.nvim';
import { CancellationToken, Position, TextDocument, WorkspaceEdit } from 'vscode-languageserver-protocol';
import { IServiceContainer } from '../ioc/types';
export declare class PythonRenameProvider implements RenameProvider {
    private serviceContainer;
    private readonly outputChannel;
    private readonly configurationService;
    constructor(serviceContainer: IServiceContainer);
    provideRenameEdits(document: TextDocument, position: Position, newName: string, _token: CancellationToken): ProviderResult<WorkspaceEdit>;
    private doRename;
}
