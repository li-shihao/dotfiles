import { IServiceContainer } from '../ioc/types';
import { Document, ExtensionContext, OutputChannel } from 'coc.nvim';
import { Range } from 'vscode-languageserver-types';
export declare function activateSimplePythonRefactorProvider(context: ExtensionContext, outputChannel: OutputChannel, serviceContainer: IServiceContainer): void;
export declare function extractVariable(extensionDir: string, textEditor: Document, range: Range, outputChannel: OutputChannel, serviceContainer: IServiceContainer): Promise<any>;
export declare function extractMethod(extensionDir: string, textEditor: Document, range: Range, outputChannel: OutputChannel, serviceContainer: IServiceContainer): Promise<any>;
