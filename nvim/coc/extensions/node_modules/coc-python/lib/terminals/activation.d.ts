import { ITerminalManager, IWorkspaceService } from '../common/application/types';
import { ITerminalActivator } from '../common/terminal/types';
import { IDisposableRegistry } from '../common/types';
import { ITerminalAutoActivation } from './types';
export declare class TerminalAutoActivation implements ITerminalAutoActivation {
    private readonly terminalManager;
    private readonly activator;
    private readonly workspaceService;
    private handler?;
    constructor(terminalManager: ITerminalManager, disposableRegistry: IDisposableRegistry, activator: ITerminalActivator, workspaceService: IWorkspaceService);
    dispose(): void;
    register(): void;
    private activateTerminal;
}
