import { Disposable } from 'vscode-languageserver-protocol';
import { ICommandManager, IDocumentManager, IWorkspaceService } from '../../common/application/types';
import { IFileSystem } from '../../common/platform/types';
export declare class DjangoContextInitializer implements Disposable {
    private documentManager;
    private workpaceService;
    private fileSystem;
    private readonly isDjangoProject;
    private workspaceContextKeyValues;
    private lastCheckedWorkspace;
    private disposables;
    constructor(documentManager: IDocumentManager, workpaceService: IWorkspaceService, fileSystem: IFileSystem, commandManager: ICommandManager);
    dispose(): void;
    private getActiveWorkspace;
    private ensureContextStateIsSet;
}
