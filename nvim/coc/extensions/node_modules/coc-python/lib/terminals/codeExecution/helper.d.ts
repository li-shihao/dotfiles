import { Uri } from 'coc.nvim';
import { IServiceContainer } from '../../ioc/types';
import { ICodeExecutionHelper } from '../types';
export declare class CodeExecutionHelper implements ICodeExecutionHelper {
    private readonly processServiceFactory;
    private readonly configurationService;
    constructor(serviceContainer: IServiceContainer);
    normalizeLines(code: string, resource?: Uri): Promise<string>;
    getFileToExecute(): Promise<Uri | undefined>;
    getSelectedTextToExecute(mode: string): Promise<string | undefined>;
}
