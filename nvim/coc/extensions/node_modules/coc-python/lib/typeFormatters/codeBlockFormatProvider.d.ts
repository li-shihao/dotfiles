import { FormattingOptions, Position, TextDocument, TextEdit } from 'vscode-languageserver-protocol';
import { BlockRegEx } from './contracts';
export declare class CodeBlockFormatProvider {
    private blockRegExp;
    private previousBlockRegExps;
    private boundaryRegExps;
    constructor(blockRegExp: BlockRegEx, previousBlockRegExps: BlockRegEx[], boundaryRegExps: BlockRegEx[]);
    canProvideEdits(line: string): boolean;
    provideEdits(document: TextDocument, position: Position, _ch: string, options: FormattingOptions, line: string): TextEdit[];
}
