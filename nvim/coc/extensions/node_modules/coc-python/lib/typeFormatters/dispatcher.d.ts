import { CancellationToken, FormattingOptions, Position, TextDocument, TextEdit } from 'vscode-languageserver-protocol';
import { OnTypeFormattingEditProvider, ProviderResult } from 'coc.nvim';
export declare class OnTypeFormattingDispatcher implements OnTypeFormattingEditProvider {
    private readonly providers;
    constructor(providers: Record<string, OnTypeFormattingEditProvider>);
    provideOnTypeFormattingEdits(document: TextDocument, position: Position, ch: string, options: FormattingOptions, cancellationToken: CancellationToken): ProviderResult<TextEdit[]>;
    getTriggerCharacters(): {
        first: string;
        more: string[];
    } | undefined;
}
