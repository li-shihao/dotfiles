import { ITag } from './contracts';
import { CancellationToken } from 'vscode-languageserver-protocol';
export interface IRegexGroup {
    name: string;
    file: string;
    code: string;
    type: string;
    line: number;
}
export declare function matchNamedRegEx(data: String, regex: String): IRegexGroup | null;
export declare function parseTags(workspaceFolder: string, tagFile: string, query: string, token: CancellationToken): Promise<ITag[]>;
