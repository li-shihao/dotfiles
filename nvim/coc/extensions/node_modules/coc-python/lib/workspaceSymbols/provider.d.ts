import { WorkspaceSymbolProvider as IWorspaceSymbolProvider } from 'coc.nvim';
import { CancellationToken, SymbolInformation } from 'vscode-languageserver-protocol';
import { ICommandManager } from '../common/application/types';
import { IFileSystem } from '../common/platform/types';
import { Generator } from './generator';
export declare class WorkspaceSymbolProvider implements IWorspaceSymbolProvider {
    private fs;
    private commands;
    private tagGenerators;
    constructor(fs: IFileSystem, commands: ICommandManager, tagGenerators: Generator[]);
    provideWorkspaceSymbols(query: string, token: CancellationToken): Promise<SymbolInformation[]>;
}
